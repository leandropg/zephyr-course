config APP_HEARTBEAT_PERIOD_MS
  int "Heartbeat Period in ms (100-2000)"
  default 500
  range 100 2000

menuconfig LED_SUBSYSTEM
  bool "LED Subsystem"
  default y

if LED_SUBSYSTEM

  choice
    prompt "LED blink sleep time"
    default LED_BLINK_SLEEP_1000MS

    config LED_BLINK_SLEEP_250MS
      bool "0.25s (lowest)"

    config LED_BLINK_SLEEP_500MS
      bool "0.5s (low)"

    config LED_BLINK_SLEEP_1000MS
      bool "1s (medium)"

    config LED_BLINK_SLEEP_2000MS
      bool "2s (high)"

  endchoice

  config BLINK_SLEEP_TIME_MS
    int
    default 250 if LED_BLINK_SLEEP_250MS
    default 500 if LED_BLINK_SLEEP_500MS
    default 1000 if LED_BLINK_SLEEP_1000MS
    default 2000 if LED_BLINK_SLEEP_2000MS
    default 0

  menuconfig ADVANCED_LED_SETTINGS
    bool "Advanced LED settings"
    default n

  if ADVANCED_LED_SETTINGS

    config LED_BRIGHTNESS
      int "LED brightness (0-100)"
      default 100
      range 0 100

    config LED_FADE_DURATION
      int "LED fade duration (ms)"
      default 500
      range 0 5000

    menu "Expert settings"
      visible if ADVANCED_LED_SETTINGS

      config LED_DEBUG_ENABLE
        bool "Enable LED debugging"

      config LED_CUSTOM_BLINK_PATTERN
        bool "Custom blink pattern"

    endmenu

    endif # ADVANCED_LED_SETTINGS

endif # LED_SUBSYSTEM

source "Kconfig.zephyr"
